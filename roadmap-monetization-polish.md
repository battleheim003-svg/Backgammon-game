# نقشه راه: اقتصاد سکه، صفحه بازی جدید، و فروشگاه

**هدف سند:** این سند برای اجرا توسط یک ایجنت کدنویسی (Antigravity) نوشته شده و باید توسط Claude Pro مرحله به مرحله نظارت و بازبینی شود. هر فاز مستقل، قابل تست، و قابل commit جداگانه است. ترتیب فازها عمدی است — فاز ۱ (رفع باگ اقتصادی) باید قبل از هر چیز دیگری انجام شود چون هر فاز بعدی روی یک اقتصاد سالم بنا می‌شود.

**قانون طلایی کل پروژه:** بازی نباید حس "پولکی" (pay-to-progress فشار بالا) بدهد. هر تصمیم قیمت‌گذاری باید در برابر این معیار سنجیده شود: *«آیا یک بازیکن معمولی که هر روز چند بازی می‌کند، بدون هیچ پرداخت واقعی، در عرض یک هفته تا یک ماه می‌تواند چیزهای جذاب را باز کند؟»* اگر جواب نه است، قیمت خیلی بالاست. اگر بازیکن در ۲-۳ روز همه‌چیز را باز کرد، قیمت خیلی پایین است و هیجان از بین می‌رود.

---

## فاز ۰ — پیش‌نیاز: خواندن اسناد موجود (فقط خواندن، بدون اجرا)

قبل از شروع، این فایل‌ها را در ریشه پروژه بخوان تا کارهای قبلاً انجام‌شده تکرار نشوند:

- `settings-overhaul-prompt.md` — **پیاده‌سازی شده** (commit `2045fd6`). کاری لازم نیست مگر پالیش بیشتر خواسته شود.
- `shop-overhaul-prompt.md` — **تا حدی پیاده‌سازی شده** (commit `fbd356b`) ولی از آن زمان drift کرده. جدول قیمت‌گذاری rarity این سند (Common 50–200 / Rare 300–700 / Epic 800–2000 / Legendary 2500–5000، بر پایه ~۱۶۰ سکه/روز درآمد بازیکن فعال) معتبر است و باید در فاز ۳ دوباره استفاده شود، نه از صفر بازتولید.
- `theme-unlock-prompt.md` — **پیاده‌سازی شده** (commit `efa7980`) اما طبق خواسته کاربر باید در فاز ۲ این سند **جایگزین** شود (از مدل تبلیغی به مدل سکه‌ای).
- `theme-woodland-impl-prompt.md` — **پیاده‌سازی شده با یک تغییر مهم**: به‌جای PNG، رندر کاملاً پروسیجرال/برداری (Canvas + gradients + paths) انتخاب شد. **این الگو باید برای تم‌های جدید هم رعایت شود.**
- `gemini-theme-prompts.md` — **اجرا نشده**. شامل پرامپت‌های تصویرسازی برای تم‌های Galaxy، Ancient Egypt، Neon Retro Arcade است. این‌ها فقط برای *الهام بصری/رنگ* استفاده شوند، نه برای تولید PNG واقعی — چون مسیر فنی انتخاب‌شده پروژه (طبق Woodland) رندر برداری است، نه بیت‌مپ.

خروجی این فاز: هیچ کد تغییر نمی‌کند. فقط باید یک خلاصه یک‌پاراگرافی از این ۵ سند در پیام commit یا PR اول نوشته شود تا Claude Pro تأیید کند که ایجنت زمینه را درست فهمیده است.

---

## فاز ۱ — اصلاح اقتصاد سکه (اولویت اول، قبل از هر فاز دیگر)

### ۱.۱ رفع باگ Rental (بحرانی)

**فایل‌ها:** `app/src/main/java/games/mrlaki5/backgammon/Economy/ShopAdapter.java`، `PlayerProfileManager.java`، احتمالاً `ShopItem.java`

**مشکل:** آیتم‌های rental (`rental_diamond`, `rental_sultan`, `rental_dragon`) با ۵۰-۱۲۰ سکه خریده می‌شوند اما هیچ‌جا expiry ذخیره یا چک نمی‌شود — عملاً معادل خرید دائمی یک آیتم Legendary با ۲٪ قیمت واقعی است.

**راه‌حل:**
- در `PlayerProfileManager`، هنگام equip کردن یک آیتم rental، یک timestamp انقضا (`System.currentTimeMillis() + 24*60*60*1000`) در SharedPreferences ذخیره شود (کلید جداگانه به ازای هر آیتم rental، مثلاً `rental_expiry_<itemId>`).
- یک متد `isRentalActive(itemId)` اضافه شود که چک می‌کند آیا هنوز منقضی نشده.
- وقتی یک rental منقضی می‌شود، اگر آیتم equip شده rental بوده، به‌صورت خودکار به آیتم پیش‌فرض (`frame_default` / `dice_default`) برگردد — این چک باید هم موقع باز شدن اپ (`MenuActivity.onResume` یا مشابه) و هم قبل از رندر هر بازی انجام شود.
- در UI شاپ (`ShopAdapter`)، روی کارت‌های rental فعال یک countdown یا برچسب "⏳ فلان ساعت مانده" نشان داده شود.
- بعد از انقضا، بازیکن باید بتواند دوباره با پرداخت ۵۰-۱۲۰ سکه، همان rental را برای ۲۴ ساعت دیگر تمدید کند (این خودش یک sink تکرارشونده خوب برای اقتصاد است — نکته مهم در ۱.۳).

### ۱.۲ کاهش سود hint/undo نسبت به win

**فایل:** `CoinConfig.java`

فعلاً `HINT_COST=20` و `UNDO_COST=30` در برابر `WIN_BASE=10` خیلی ارزان‌اند (بازیکن با یک برد می‌تواند نصف یک hint خرید کند، اما در عمل این اعداد به‌طور مستقل از win محاسبه نمی‌شوند و ریسک این است که بازیکن به‌جای فکر کردن، هر بار hint/undo بخرد). پیشنهاد:
- `HINT_COST` → 25 (بدون تغییر شدید، فقط کمی سخت‌گیرتر)
- `UNDO_COST` → 40، و اضافه کردن یک escalating cost در همان بازی (اولین undo در یک بازی 40، دومین 60، سومین به بعد 90) تا از استفاده مکرر جلوگیری شود بدون اینکه استفاده یک‌باره را گران کند.
این تغییر باید عددی و کوچک باشد — **هدف تنبیه بازیکن نیست، هدف حفظ ارزش استراتژیک بازی است.**

### ۱.۳ حذف کامل constant های مرده تم از شاپ

**فایل:** `CoinConfig.java`

`THEME_POP_ART_PRICE` و مشابه‌ها که از commit `efa7980` بلااستفاده مانده‌اند حذف شوند — چون در فاز ۲ این پروژه دوباره برای تم‌ها قیمت سکه‌ای تعریف می‌کنیم، بهتر است این ثابت‌های قدیمی و اسم‌های تکراری پاک شوند تا تداخل نامی پیش نیاید.

### ۱.۴ تعریف بودجه اقتصادی رسمی (سند مرجع)

قبل از قیمت‌گذاری فاز ۲ و ۳، یک جدول درآمد/هزینه روزانه باید در همین فایل (یا فایلی مثل `docs/economy-budget.md`) مستند شود، بر پایه اعداد واقعی که در بخش ممیزی به‌دست آمد:
- بازیکن معمولی (۲-۳ بازی در روز، بدون تماشای تبلیغ اضافه): ~۴۰-۶۰ سکه/روز از بردها + پاداش‌های روزانه
- بازیکن فعال (با ad-watch و چالش‌ها): ~۱۵۰-۱۸۰ سکه/روز (رقمی که در `shop-overhaul-prompt.md` هم آمده)

این دو عدد، سقف قیمت‌گذاری فاز ۲ (تم‌ها) و فاز ۳ (آیتم‌های جدید شاپ) را تعیین می‌کند: **هیچ آیتم پایه نباید بیش از ۷-۱۰ روز طول بکشد**، و آیتم‌های نادر/افسانه‌ای نباید بیش از ۳-۴ هفته.

---

## فاز ۲ — بازطراحی کامل صفحه بازی جدید (New Game Dialog)

**فایل‌ها:** `dialog_single_player.xml`, `pass_and_play_dialog.xml`, `MenuActivity.java` (متدهای `showNewGameDialog`, `showPassAndPlayDialog`, `setupThemePicker`, `updateThemeSelection`, `updateDifficultySelection`)

### ۲.۱ از دیالوگ ساده به صفحه اختصاصی

پیشنهاد: به‌جای `AlertDialog` با `LinearLayout` inflate شده، این به یک `DialogFragment` تمام‌صفحه یا نیمه‌صفحه (bottom sheet استایل، با انیمیشن slide-up) تبدیل شود. این باعث می‌شود فضای کافی برای ۸ تم (۲ ردیف ۴تایی که الان جا خوش کرده) و طراحی بهتر وجود داشته باشد بدون فشردگی.

### ۲.۲ سیستم بصری فانتزی

- یک فونت display/fantasy سفارشی (نه `sans-serif-medium` پیش‌فرض) برای تیتر «بازی جدید»، نام سختی‌ها، و برچسب‌های تم انتخاب شود (فونتی با حس فانتزی/رویال متناسب با تم Royal/Luxury فعلی بازی — می‌توان از فونت‌های آزاد Google Fonts مثل Cinzel, MedievalSharp, یا Philosopher استفاده کرد که با حس تخته‌نرد سلطنتی هم‌خوانی دارد).
- پالت رنگی هاردکد پراکنده در XML (`#E0E0E0`, `#F4B044` و...) جمع‌آوری و به `colors.xml` منتقل شود تا با بقیه صفحات (`activity_settings.xml`) هماهنگ شود.
- دکمه‌ها (`Cancel`/`Play`) از `Button` ساده به `MaterialButton` با گرادینت طلایی، سایه، و حالت press با اسکیل کوچک (۰.۹۵x) و ریپل رنگی ارتقا یابند — نه دکمه مستطیل تخت.
- آیکون قفل 🔒 امکجی جایگزین شود با یک آیکون برداری سفارشی (svg/vector drawable) هماهنگ با استایل بازی.

### ۲.۳ انیمیشن و فیدبک

- ورود دیالوگ: انیمیشن fade+scale یا slide-up (نه ظاهر شدن ناگهانی).
- انتخاب تم: به‌جای صرفاً عوض شدن border drawable، یک انیمیشن scale-bounce کوتاه (۱۵۰-۲۰۰ms) روی thumbnail انتخاب‌شده اضافه شود (از همان الگوی `OvershootInterpolator` که در `MenuActivity.polishMenuButtons()` برای دکمه‌های منوی اصلی استفاده شده — این الگو از قبل در پروژه هست، فقط به این دیالوگ نرسیده).
- انتخاب سختی: مشابه، یک واکنش لمسی کوچک.
- Thumbnail های تم بزرگ‌تر شوند (فعلاً ۵۲-۵۴dp، به حداقل ۷۲-۸۰dp افزایش یابد) تا جزئیات تم بهتر دیده شود، خصوصاً با ۸ تم به‌جای ۵.

### ۲.۴ گسترش شبکه تم به ۸ (۴ رایگان + ۴ سکه‌ای)

**این بخش مستقیماً به فاز اقتصاد وصل است، با دقت اجرا شود:**

تم‌های موجود: Royal(0), PopArt(1), Cyberpunk(2), Luxury(3), Woodland(4). سه تم جدید باید اضافه شوند (طبق `gemini-theme-prompts.md`: Galaxy/Deep Space(5), Ancient Egypt(6), Neon Retro Arcade(7)) تا به ۸ برسیم.

**مدل آنلاک جدید (جایگزین مدل تبلیغی فعلی که `theme-unlock-prompt.md` پیاده کرده بود):**
- ۴ تم رایگان (بدون تبلیغ، بدون سکه، از همان اول باز): Royal، و ۳ تای دیگر که باید بر اساس محبوبیت/تنوع بصری انتخاب شوند — پیشنهاد: Royal, Woodland, PopArt, Cyberpunk (ترکیبی از سبک کلاسیک و مدرن تا سلیقه‌های مختلف روز اول راضی باشند).
- ۴ تم سکه‌ای: Luxury، Galaxy، Ancient Egypt، Neon Retro — قیمت‌گذاری بر اساس جدول فاز ۱.۴، پیشنهاد ۴۰۰-۸۰۰ سکه هرکدام (۳-۵ روز بازی فعال) تا در دسترس باشند اما هدف داشتن ایجاد کنند.
- گزینه تبلیغ (`RewardedAdPlacement.THEME_UNLOCK`) به‌جای آنلاک دائمی رایگان، به یک **تخفیف موقت** تبدیل شود (مثلاً "با دیدن یک تبلیغ، ۵۰٪ تخفیف روی این تم تا ۱ ساعت آینده بگیر") — این هم مدل تبلیغی حفظ می‌شود (که برای درآمد اپ لازم است) هم اقتصاد سکه بی‌معنی نمی‌شود. **این یک تصمیم طراحی مهم است — قبل از اجرا با کاربر (مالک پروژه) تأیید بگیر، چون ممکن است ترجیح بدهد ad-unlock کاملاً حذف شود.**
- پیاده‌سازی: در `GamePreferences`، منطق `isThemeUnlocked` باید یک شرط جدید اضافه کند: تم‌های id 0،4،1،2 (یا هر ۴تای انتخابی) همیشه true برگردانند؛ بقیه باید چک `hasPurchasedTheme(themeId)` از `PlayerProfileManager`/`CoinManager` را انجام دهند (که باید بازنویسی/احیا شود چون در `efa7980` این منطق خرید سکه‌ای تم حذف شده بود).

### ۲.۵ اسلات‌های ۵-۷ در XML

خوشبختانه `theme-woodland-impl-prompt.md` از قبل شبکه ۲×۴ (۸ اسلات) را در هر دو دیالوگ آماده کرده و اسلات‌های 5-7 با `visibility="gone"` پنهانند — فقط باید `visibility` به `visible` تغییر کند و thumbnail و overlay قفل/قیمت برای هرکدام اضافه شود.

---

## فاز ۳ — عمیق‌تر کردن فروشگاه

**فایل‌ها:** `CoinShopActivity.java`, `ShopAdapter.java`, `ShopItem.java`

### ۳.۱ رفع drift کاتالوگ

`shop-overhaul-prompt.md` هنوز از دسته `THEME` و آیتم‌های `rental_cyberpunk`/`rental_luxury` یاد می‌کند که در کد فعلی وجود ندارند. این سند باید در همین فاز به‌روزرسانی یا با یک نوت "منسوخ — نگاه کن به roadmap-monetization-polish.md" جایگزین شود تا سردرگمی آینده ایجاد نشود.

### ۳.۲ افزودن دسته «مصرفی» (Consumables) — مهم‌ترین بخش برای «ارزش خرج کردن»

فعلاً همه آیتم‌های شاپ یک‌بار خریداری و برای همیشه‌اند — بعد از تکمیل کالکشن، دلیلی برای خرج سکه باقی نمی‌ماند. پیشنهاد آیتم‌های مصرفی تکرارشونده:
- **بسته Hint** (۳ عدد hint اضافه، معادل صرفه‌جویی در قیمت تک‌تک): ۵۰ سکه برای ۳ عدد (به‌جای ۲۵×۳=۷۵)
- **بسته Undo مشابه**
- **جلوه بصری تاس** (VFX کوتاه هنگام رول تاس — کاملاً کازمتیک، مصرف‌نشدنی ولی قابل equip/تعویض مثل dice skin — این یک دسته جدید کازمتیک است نه مصرفی، برای تنوع)
- **موسیقی/آمبیانس تخته** (پک صوتی اختیاری برای هنگام بازی، اگر زیرساخت صوتی پروژه اجازه دهد)

این‌ها باید ارزان و تکرارشونده باشند (۳۰-۸۰ سکه) تا بعد از تکمیل کالکشن‌های اصلی، بازیکن همچنان دلیلی برای بازی کردن و جمع کردن سکه داشته باشد — **این دقیقاً همان چیزی است که "ارزش خرج کردن" واقعی می‌سازد.**

### ۳.۳ بسته‌های ترکیبی (Bundle)

مثلاً "بسته شروع فصل": ۱ frame + ۱ dice skin هم‌رنگ با ۱۵٪ تخفیف نسبت به خرید جداگانه. این باعث حس ارزش بیشتر می‌شود بدون اینکه قیمت پایه اقلام تغییر کند.

### ۳.۴ نشانگر پیشرفت برای unlockRequirement

آیتم‌هایی که علاوه بر سکه، نیازمند تعداد برد مشخص‌اند (`unlockRequirement`)، به‌جای فقط قفل قرمز، باید یک progress bar کوچک نشان دهند ("۷ از ۱۰ برد") — این بازیکن را برای رسیدن به هدف ترغیب می‌کند به‌جای گیج کردنش.

### ۳.۵ ارتقای UI از GridView/BaseAdapter

`GridView`+`BaseAdapter` یک الگوی قدیمی Android است. تبدیل به `RecyclerView` با `GridLayoutManager` و انیمیشن‌های ساده هنگام خرید/equip (fade/scale روی کارت هنگام تغییر state) — این کار مستقل از منطق اقتصادی است و می‌تواند در انتها یا موازی انجام شود.

---

## ترتیب اجرا پیشنهادی برای Antigravity

1. فاز ۱ کامل (۱.۱ تا ۱.۴) — commit جدا، تست دستی: خرید rental، صبر شبیه‌سازی‌شده (تغییر ساعت سیستم یا mock کردن زمان در تست) و چک شدن انقضا.
2. فاز ۲.۱ تا ۲.۳ (بازطراحی بصری بدون تغییر منطق آنلاک) — commit جدا، قابل تست بصری فوری در امولاتور.
3. فاز ۲.۴ و ۲.۵ (۳ تم جدید + مدل آنلاک سکه‌ای) — این بزرگ‌ترین فاز است، بهتر است به ۲ زیر-commit تقسیم شود: (الف) پیاده‌سازی ۳ کلاس `BoardTheme` جدید و تست رندر مستقل از UI، (ب) اتصال به دیالوگ و اقتصاد.
4. فاز ۳ — می‌تواند موازی با فاز ۲ توسط یک نشست جدا پیش برود چون فایل‌های متفاوتی را لمس می‌کند.

هر فاز باید قبل از فاز بعد توسط Claude Pro از نظر: (الف) عدم شکستن اقتصاد موجود، (ب) هم‌خوانی بصری با تم‌های قبلی، (ج) build سالم بازبینی شود.

---

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
